#include<stdio.h>
#include<math.h>

int main()
{
    int i,j;
    int a,b,t;
    while(scanf("%d",&a)==1)
    {
        while(scanf("%d",&t)==1)
        printf("%d\n",(int)sqrt(t));
    }

    return 0;
}
